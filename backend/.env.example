# VoiceNotes 后端环境变量示例
# 用法：复制本文件为 .env（同目录），填入真实值。.env 已被 .gitignore 排除，不会进 git。
# 切勿把真实密钥提交到仓库。

# ---- DashScope（转写 Paraformer + 概括 Qwen，一个 key 通用）----
# 申请：https://dashscope.console.aliyun.com/  -> API-KEY 管理 -> 创建
DASHSCOPE_API_KEY=sk-在这里填你的key

# ---- TTS 朗读（speech-2.8-hd，OpenAI 兼容中转站）----
# base url 形如 https://你的中转站/v1
TTS_BASE_URL=https://在这里填中转站baseurl/v1
TTS_API_KEY=在这里填中转站key
# 可选：朗读音色（默认 alloy）
TTS_VOICE=alloy

# ---- MySQL（本机已运行的数据库）----
DB_USER=root
DB_PASSWORD=在这里填你的MySQL密码

# ---- JWT 签名密钥（启动脚本会在未设置时自动生成随机值，一般无需手填）----
# JWT_SECRET=
